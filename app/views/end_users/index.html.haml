%h3 Add User
= render 'users/form'

- content_for :sidebar do
  %h3 
    %i.icon-user
    Following Users
  %ul
    - @users.each do |u|
      %li
        = u.username
        = link_to 'Destroy', u, method: :delete, confirm: "Are you sure you want to stop following \"#{u.username}\""

%h3 Posts
%ul
- @posts.each do |p|
  = render "posts/post", post: p
